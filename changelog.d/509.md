<!-- section: Fixed -->
- Three path edge cases are fixed:
  - scan paths that share only `/` no longer produce a `//` board path;
  - `init` quotes `root` and `scan_paths` so `~` or `a: b` load back as written;
  - a hook `log` action path of `~/…` writes under the home directory (#509).
