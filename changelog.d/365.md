<!-- section: Fixed -->
- A theme file that is empty (`{}` or blank), or has nothing left once its bad sections
  are ignored, no longer shadows the built-in theme: it falls through with one warning
  ("is empty"), and a custom one counts as unknown. A dangling-symlink override now warns
  instead of being skipped silently (#365).
