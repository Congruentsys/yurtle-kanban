<!-- section: Fixed -->
- `QueryEngine.structured_query` skips status and type values that can't be IRIs (spaces,
  quotes, `<>{}|\\^`) instead of building them, so rdflib no longer logs "does not look
  like a valid URI"; such a value still matches nothing (#367).
