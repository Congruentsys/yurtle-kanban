<!-- section: Fixed -->
- **"Unknown priority" now shows the value the same way everywhere** (#190). The
  service quoted a string (`'urgent'`) while MCP and the CLI didn't. Strings now
  appear as typed and other values as their repr (`5`, `True`, `['high']`).
  `unknown_priority_message` does the rendering itself.
